class Solution {
public:
    void solve(vector<vector<char>>& board) {
    	int m = board.size();
    	if(!m) return;
    	int n = board[0].size();
    	if(!n) return;

        map<pair<int,int>,int> regionid;
        set<pair<int,int> > pointsinregion;

    }
    bool isRegion(pair<int,int> firstp,vector<vector<char>>& board){
    	if()
    }
};